# INSTALACIÓN MODELOS NUEVOS

## 1. CREAR APP ALERTAS
mkdir apps/alertas
touch apps/alertas/__init__.py
cp models_alertas.py apps/alertas/models.py
touch apps/alertas/admin.py apps/alertas/apps.py

## 2. AGREGAR CAMPOS A MODELOS EXISTENTES

### Usuario (apps/usuarios/models.py):
# Agregar campo firma según models_modificaciones_grupos.py

### Paciente (apps/procedimientos/models.py):
# Agregar: codigo_enfermedad (FK a CodigoEnfermedad)
# Deprecar: numero_historia_clinica (null=True, blank=True)

### AsignacionGrupo (apps/grupos/models.py):
# Agregar campos según models_modificaciones_grupos.py

## 3. AGREGAR MODELOS NUEVOS

### En apps/procedimientos/models.py:
- CodigoEnfermedad
- ValoracionProfesional  
- AdmisionTerapia
- AsistenciaSesion

### En apps/grupos/models.py:
- AlertaCupo

### En apps/alertas/models.py:
- AlertaInasistencia
- ConfiguracionAlerta

## 4. SETTINGS.PY
INSTALLED_APPS = [
    ...
    'apps.alertas',
]

MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

## 5. URLS.PY (principal)
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    ...
] + static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)

## 6. MIGRACIONES
python manage.py makemigrations
python manage.py migrate

## 7. CARGAR CIE-10
mkdir -p apps/procedimientos/management/commands
touch apps/procedimientos/management/__init__.py
touch apps/procedimientos/management/commands/__init__.py
cp cargar_cie10.py apps/procedimientos/management/commands/
python manage.py cargar_cie10

## 8. ADMIN (opcional)
# Registrar modelos nuevos en admin.py de cada app
